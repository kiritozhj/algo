lowbit求一个数由哪些2次幂组成
```cpp
// 求一个数的二进制最低有效位
int lowbit(int x)
{
    return x&-x;
}

vector<int> bin(int x)
{
    vector<int>ans;
    while(x)
    {
        int tmp = lowbit(x);
        ans.push_back(tmp);
        x ^= tmp;
    }
    return ans;
}
```